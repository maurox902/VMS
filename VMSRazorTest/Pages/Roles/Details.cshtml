@page
@model VMSRazorTest.Pages.User.Roles.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Role.description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Role.description)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Role?.role_id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
